spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/galazel/config-repo
          default-label: main

server:
  port: 8888
