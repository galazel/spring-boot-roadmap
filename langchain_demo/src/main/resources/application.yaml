spring:
  application:
    name: langchain_demo

langchain4j:
  github:
    chat-model:
      git-hub-token: ${GITHUB_AI_TOKEN}
      model-name: gpt-4o-mini
      temperature: 0.7
      max-tokens: 1000